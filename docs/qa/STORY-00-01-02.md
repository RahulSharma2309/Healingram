# QA — STORY-00-01-02 GitHub Actions CI

**Environment:** localhost (same commands as `.github/workflows/ci.yml`)  
**Branch:** `story/STORY-00-01-02-github-actions-ci`  
**Date:** 12 September 2026

## What I ran

```text
npm run build
dotnet test backend/Healingram.slnx -c Release
```

## Acceptance

| Criterion | Result | Evidence |
| --- | --- | --- |
| Frontend build is part of CI | Pass | Workflow `frontend-build` job runs `npm ci` and `npm run build`. Local `npm run build` succeeded (`tsc -b && vite build`). |
| Backend build + unit tests are part of CI | Pass | Workflow `backend-build-and-test` runs restore, build, `dotnet test`. Local test run: 12 passed, 0 failed. |
| Red tests fail the workflow | Pass | Workflow has no `continue-on-error`. `dotnet test` non-zero exit fails the GitHub job. Unit tests `CiWorkflowTests` lock this contract. |
| Triggers on PRs into the iteration branch | Pass | `on.pull_request.branches` includes `feature/v1-iteration-1`. |

## Defects

None for this story.

**Note:** GitHub has not executed the workflow yet because this story branch has not been pushed. Local equivalent is green. A GitHub run is the remaining confirmation after push + PR.

## Feature gate

FEAT-00-01 is not complete until STORY-00-01-01 is also `done`.
