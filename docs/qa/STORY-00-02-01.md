# QA — STORY-00-02-01 API host + module interface

**Environment:** localhost (`dotnet test`, existing API on `:5080`)  
**Branch:** `story/STORY-00-02-01-api-host`  
**Date:** 12 September 2026  
**Result:** `qa_passed` (tests). Live host still needs a restart to pick up Catalog `/ready`.

## What I ran

```text
dotnet test backend/Healingram.slnx -c Release
curl http://localhost:5080/api/health
curl http://localhost:5080/api/meta
curl http://localhost:5080/api/identity/ready
```

## Acceptance

| Criterion | Result | Evidence |
| --- | --- | --- |
| `GET /api/health` JSON | Pass | Host test + live `:5080` |
| `GET /api/meta` JSON | Pass | Host test + live `:5080` |
| Modules via `IAppModule` | Pass | `Host_registers_eight_app_modules` |
| Module `/ready` routes | Pass | 8 host tests. Catalog `/ready` is new — restart the running API before live UAT of that one path. |
| Tests do not need Postgres | Pass | Factory sets `Schema:ApplyOnStartup=false` |

## Defects

None blocker. Minor: the long-running `dotnet run` process was started before this story; restart it before clicking Catalog `/ready` in the browser.
