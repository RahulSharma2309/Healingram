# UAT — STORY-00-01-02 GitHub Actions CI

**Status:** `uat_passed` locally (PO agent). Human still needs to approve merge after the first green GitHub check.

## What I checked as PO

- A developer can open a PR into `feature/v1-iteration-1` and CI will try to build the website and the backend, then run tests.
- If the website does not build, CI fails.
- If a unit test fails, CI fails. We do not “warn and continue.”
- This does not change any customer screen.

## Result

Pass for the local pipeline. After you push the story branch, glance at the GitHub Actions tab once. If that check is green, this story is fully done.
