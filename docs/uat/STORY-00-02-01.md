# UAT — STORY-00-02-01 API host + module interface

**Status:** ready for you — click the three URLs on your laptop  
**Date:** 12 September 2026

## What to check

1. http://localhost:5080/api/health — `status` is `ok`
2. http://localhost:5080/api/meta — `service` is `healingram-api`
3. http://localhost:5080/api/identity/ready — `{ "module": "Identity" }`

If the API was already running before this story, restart it, then also open http://localhost:5080/api/catalog/ready

This does not change any customer screen.

## Result

Pending your “yes, this is what I wanted.”
